import { IHttpErrorPayload } from "@fpx/core"

export interface  Accountsgroup {
       productGCode:string, 
       productDesc:string, 
       productCat:string, 
       defaultProduct:string 
  }
  
 
