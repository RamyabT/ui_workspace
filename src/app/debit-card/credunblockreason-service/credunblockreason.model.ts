import { IHttpErrorPayload } from "@fpx/core"

export interface  Credunblockreason {
       cardType:string, 
       blockReason:string, 
       unblockReason:string, 
       unblockReasonDesc:string 
  }
  
 
