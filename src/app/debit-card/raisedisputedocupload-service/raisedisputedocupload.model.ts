import { IHttpErrorPayload } from "@fpx/core"

export interface  Raisedisputedocupload {
       inventoryNumber:string, 
       serialNo:number, 
       docInvNumber:string, 
       fileName:string 
  }
  
 
